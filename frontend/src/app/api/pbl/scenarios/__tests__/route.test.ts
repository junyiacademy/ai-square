import { GET } from '../route';

// Skip all tests as the implementation has changed to database-only
describe.skip('/api/pbl/scenarios', () => {
  it('skipped - implementation changed to database-only', () => {
    expect(true).toBe(true);
  });
});